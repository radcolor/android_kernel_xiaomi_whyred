version: 2.1
jobs:
  oldcam:
   docker:
      - image: archlinux:latest
   steps:
      - run:
          command: |
           pacman -Sy base-devel bc llvm curl wget git --noconfirm
           git clone --depth=1 https://github.com/theradcolor/android_kernel_xiaomi_whyred.git -b $CIRCLE_BRANCH source
           cd source
           git clone --depth=1 https://github.com/theradcolor/lazyscripts.git -b master
           sed -i "s/token=.*/token=$TG_BOT_API/g" lazyscripts/ci-scripts/ci-rad
           script -c "bash -x lazyscripts/ci-scripts/ci-rad --oldcam" build.log
  newcam:
   docker:
      - image: archlinux:latest
   steps:
      - run:
          command: |
           pacman -Sy base-devel bc llvm curl wget git --noconfirm
           git clone --depth=1 https://github.com/theradcolor/android_kernel_xiaomi_whyred.git -b $CIRCLE_BRANCH source
           cd source
           git clone --depth=1 https://github.com/theradcolor/lazyscripts.git -b master
           sed -i "s/token=.*/token=$TG_BOT_API/g" lazyscripts/ci-scripts/ci-rad
           script -c "bash -x lazyscripts/ci-scripts/ci-rad --newcam" build.log
  fakejob:
   docker:
      - image: archlinux:latest
   steps:
      - run:
          command: |
           pacman -Sy base-devel bc llvm curl wget git --noconfirm
           git clone --depth=1 https://github.com/theradcolor/android_kernel_xiaomi_whyred.git -b $CIRCLE_BRANCH source
           cd source
           git clone --depth=1 https://github.com/theradcolor/lazyscripts.git -b master
           sed -i "s/token=.*/token=$TG_BOT_API/g" lazyscripts/ci-scripts/ci-fake
           script -c "bash -x lazyscripts/ci-scripts/ci-fakr" build.log
  nulljob:
   docker:
      - image: archlinux:latest
   steps:
      - run:
          command: |
           echo "Null job!"
workflows:
  version: 2.1
  BOB the builder:
    jobs:
      - nulljob